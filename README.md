保存：将上面的代码保存为 index.html。

运行：双击用 Chrome 或 Edge 浏览器打开。

授权：浏览器会询问是否允许使用摄像头，点击“允许”。

操作：

等待加载（屏幕中间的字消失）。

旋转：把你的手举到摄像头前，向左向右移动手掌，地球会跟着你的手转动。

缩放：做出 👌 (OK手势) 或者是“捏东西”的动作。

两指张开：地球变大（拉近）。

两指捏合：地球变小（拉远）。
